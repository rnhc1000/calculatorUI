import React from "react";

// eslint-disable-next-line react-refresh/only-export-components
export default () => {
  return (
    <button style={{backgroundColor: "blue"}} onClick={() => window.alert('Button Clicked!')}>🚮</button>
  );
}